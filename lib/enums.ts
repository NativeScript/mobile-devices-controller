export enum Platform {
    ANDROID = "android",
    IOS = "ios"
}

export enum DeviceType {
    SIMULATOR = "simulator",
    EMULATOR = "emulator",
    DEVICE = "device",
}

export enum Status {
    SHUTDOWN = "shutdown",
    BOOTED = "booted",
    FREE = "free",
    BUSY = "busy",
    UNAUTORIZED = "unautorized",
}