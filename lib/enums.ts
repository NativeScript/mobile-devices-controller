export enum Platform {
    ANDROID = "android",
    IOS = "ios"
}

export enum DeviceType {
    SIMULATOR = "simulator",
    EMULATOR = "emulator",
    DEVICE = "device",
}

export enum Status {
    SHUTDOWN = "shutdown",
    BOOTED = "booted",
    BUSY = "busy",
    UNAUTORIZED = "unautorized",
    INVALID = "invalid"
}